<div align="center">


Live deployment: https://cliniq-aaja.onrender.com

For Render, set `GEMINI_API_KEY` in the service environment variables. Do not commit API keys to this repository.

# Run and deploy your AI Studio app


## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
# CliniQ
